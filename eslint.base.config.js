// @ts-check
// Used by `pnpm test` to lint base fixtures without React plugins,
// validating that the `base` named export works independently.
import { base } from "./index.js";

export default base;

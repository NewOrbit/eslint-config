// Covers: import/order with newlines-between always-and-inside-groups
// Two external packages separated by a blank line as required by the rule.

import eslint from "@eslint/js";

import globals from "globals";

export { eslint, globals };
